- name: Build and push Docker image
  uses: docker/build-push-action@v4
  with:
    context: ./docker      # folder where Dockerfile exists
    file: ./docker/Dockerfile
    push: true
    tags: ${{ secrets.DOCKER_USERNAME }}/workflow:latest
